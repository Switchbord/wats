# dependencies
node_modules/

# build output
/dist/
dist/
coverage/
*.tsbuildinfo
.typedoc-out/

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun-debug.log*

# local env / secrets
.env
.env.*
!.env.example
*.local


# generated local WATS config; examples belong under examples/
wats.config.yaml
wats.config.yml
wats.config.json
# ...but the Railway deploy placeholder config (env-secret refs only, no real
# secrets) must ship in the build context.
!deploy/railway/wats.config.yaml

# OS / editor
.DS_Store
.vscode/
.idea/

# Local Hermes planning scratch (not part of the package)
.hermes/
